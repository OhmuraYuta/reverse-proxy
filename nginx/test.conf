server {
    listen 80; 
    server_name yt-downloader.ohmura.f5.si;
    
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    # location / { 
    #     proxy_pass http://youtube_downloader:8000;
    # }   
}